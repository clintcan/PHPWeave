# BunkerWeb WAF Local/Internal Configuration
# For development, testing, and internal networks (no SSL/domain required)
#
# USAGE:
# 1. Copy this file: cp .env.bunkerweb-local.sample .env
# 2. Edit values below (optional - defaults work out of the box!)
# 3. Start: docker compose -f docker-compose.bunkerweb-local.yml up -d
#
# Access:
# - PHPWeave: http://localhost
# - Admin UI: http://localhost:7000
# - phpMyAdmin: http://localhost:8081

# ============================================
# PORTS (Optional - change if ports conflict)
# ============================================
# HTTP port for PHPWeave access
HTTP_PORT=80

# Admin UI port
ADMIN_UI_PORT=7000

# phpMyAdmin port
PHPMYADMIN_PORT=8081

# ============================================
# BUNKERWEB ADMIN CREDENTIALS
# ============================================
# Username for BunkerWeb admin UI (http://localhost:7000)
BW_ADMIN_USER=admin

# Password for BunkerWeb admin UI - CHANGE THIS!
BW_ADMIN_PASSWORD=changeme

# BunkerWeb database password - CHANGE THIS!
BW_DB_PASSWORD=changeme

# ============================================
# PHPWEAVE APPLICATION SETTINGS
# ============================================
# Environment mode
DOCKER_ENV=development
PHPWEAVE_ENV=development

# ============================================
# DATABASE CONFIGURATION
# ============================================
# MySQL root password - CHANGE THIS!
MYSQL_ROOT_PASSWORD=rootpassword

# PHPWeave database name
DB_NAME=phpweave

# PHPWeave database user
DB_USER=phpweave_user

# PHPWeave database password - CHANGE THIS!
DB_PASSWORD=phpweave_pass

# Database host (internal Docker hostname)
DB_HOST=db

# Database charset
DB_CHARSET=utf8mb4

# Database port
DB_PORT=3306

# Database driver
DB_DRIVER=pdo_mysql

# ============================================
# PHPWEAVE FRAMEWORK SETTINGS
# ============================================
# Enable database connectivity (1 = enabled, 0 = disabled)
ENABLE_DATABASE=1

# Debug mode (0 = production, 1 = development)
DEBUG=1

# Cache driver (apcu, file, memory, redis, memcached)
CACHE_DRIVER=apcu

# Cache prefix (prevents collisions)
CACHE_PREFIX=phpweave_

# Default cache TTL in seconds (3600 = 1 hour)
CACHE_DEFAULT_TTL=3600

# Enable cache statistics (0 = disabled, 1 = enabled)
CACHE_STATS=1

# Session driver (file, database)
SESSION_DRIVER=file

# Session lifetime in seconds (1800 = 30 minutes)
SESSION_LIFETIME=1800

# ============================================
# SECURITY SETTINGS (Optional)
# ============================================
# Rate limiting (requests per second)
# Default: 100r/s (relaxed for internal use)
RATE_LIMIT=100r/s

# Enable bot protection (yes/no)
# Default: no (not needed for internal use)
USE_BOT_PROTECTION=no

# ============================================
# NOTES
# ============================================
# 1. No domain or SSL configuration needed!
# 2. Perfect for: Development, testing, internal networks
# 3. Access via http://localhost (or http://server-ip on LAN)
# 4. Admin UI: http://localhost:7000 (or http://server-ip:7000)
# 5. Change default passwords before use
# 6. For production with SSL, use docker-compose.bunkerweb.yml instead
